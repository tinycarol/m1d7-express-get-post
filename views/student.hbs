<h1>👋 This is {{username}}'s profile! 👋</h1>
<p>The full name is {{name}} {{lastName}}</p>

{{#if image}}
  <div
    class="student-image"
    alt="{{name}}'s image"
    style="background-image: url('{{image}}')"
  ></div>
{{/if}}